Dependencies

Installation

usage: RBSA.py [-h] [--ref REF] [--scf SCF]

optional arguments:
  -h, --help  show this help message and exit
  --ref REF   This is reference genome sequences which has anchored to the
              chromosomal level
  --scf SCF   This is the scaffold sequences

